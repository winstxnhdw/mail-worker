name: Test

on:
  push:
    paths:
      - .github/workflows/main.yml
      - tsconfig.json
      - package.json
      - bun.lockb
      - src/*

jobs:
  test:
    uses: winstxnhdw/actions/.github/workflows/bun.yml@main
    with:
      disable-test: true
      disable-build: true
